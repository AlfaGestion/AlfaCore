@page "/auditoria"
@page "/{idweb}/{idbase:int}/auditoria"
@inject AlfaCore.Services.IAuditoriaService AuditoriaSvc
@inject AlfaCore.Services.IAppUiOperationService UiOps

<PageTitle>AuditorÃ­a | Alfa GestiÃ³n</PageTitle>

@if (_loading)
{
    <div class="consultas-loading">
        <i class="bi bi-hourglass-split"></i> Cargando auditorÃ­aâ€¦
    </div>
}
else
{
    <div class="page-grid">
        <section class="page-intro">
            <div>
                <h2 class="page-intro__title">AuditorÃ­a</h2>
                <p class="page-intro__text">CentralizÃ¡ la revisiÃ³n de errores tÃ©cnicos y el anÃ¡lisis de actividad sospechosa de usuarios sobre comprobantes comerciales.</p>
            </div>
            <div class="actions">
                <a class="btn btn--primary" href="/auditoria/errores">
                    <i class="bi bi-bug"></i>
                    <span>AuditorÃ­a de errores</span>
                </a>
                <a class="btn btn--ghost" href="/auditoria/usuarios">
                    <i class="bi bi-shield-exclamation"></i>
                    <span>AuditorÃ­a de usuarios</span>
                </a>
            </div>
        </section>

        <section class="two-grid">
            <article class="panel-card">
                <div class="panel-card__header">
                    <div>
                        <h3 class="panel-card__title">AuditorÃ­a de errores</h3>
                        <p class="panel-card__subtitle">BitÃ¡cora tÃ©cnica actual sobre `AUX_ERR`, sin cambios en su funcionamiento.</p>
                    </div>
                    <a class="action-link" href="/auditoria/errores">Abrir</a>
                </div>
                <p class="page-intro__text">Ideal para soporte: procesos fallidos, errores SQL, integraciones y detalle tÃ©cnico por incidente.</p>
            </article>
            <article class="panel-card">
                <div class="panel-card__header">
                    <div>
                        <h3 class="panel-card__title">AuditorÃ­a de usuarios</h3>
                        <p class="panel-card__subtitle">Nueva vista operativa para detectar fraudes, remitos sin facturar y actividad irregular.</p>
                    </div>
                    <a class="action-link" href="/auditoria/usuarios">Abrir</a>
                </div>
                <p class="page-intro__text">ConsolidÃ¡ comprobantes iniciados y no grabados, remitos parciales, bajas, actividad fuera de horario y accesos sensibles.</p>
            </article>
        </section>

        @if (_feedback is not null)
        {
            <div class="conversations-inline @GetFeedbackClass(_feedback)">
                <strong>@_feedback.Title</strong>
                <div>@_feedback.Message</div>
                @if (_feedback.HasCode)
                {
                    <div>CÃ³digo: @_feedback.Code</div>
                }
                @if (_feedback.HasSuggestion)
                {
                    <div>@_feedback.Suggestion</div>
                }
            </div>
        }

        <section class="kpi-grid">
            <article class="kpi-card">
                <div class="kpi-card__label">Errores hoy</div>
                <div class="kpi-card__value">@_resumen.ErrorsToday</div>
            </article>
            <article class="kpi-card">
                <div class="kpi-card__label">Ãšltimos 7 dÃ­as</div>
                <div class="kpi-card__value">@_resumen.ErrorsLast7Days</div>
            </article>
            <article class="kpi-card">
                <div class="kpi-card__label">Proceso mÃ¡s frecuente</div>
                <div class="kpi-card__value">@_resumen.TopProcess</div>
            </article>
            <article class="kpi-card">
                <div class="kpi-card__label">Usuario mÃ¡s afectado</div>
                <div class="kpi-card__value">@_resumen.TopUser</div>
            </article>
        </section>

        <section class="three-grid">
            <article class="panel-card">
                <div class="panel-card__header">
                    <div>
                        <h3 class="panel-card__title">Errores por dÃ­a</h3>
                        <p class="panel-card__subtitle">Ãšltimas dos semanas.</p>
                    </div>
                </div>
                <div class="table-wrap">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>DÃ­a</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in _resumen.ErrorsByDay)
                            {
                                <tr>
                                    <td>@item.Label</td>
                                    <td>@item.Value</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </article>

            <article class="panel-card">
                <div class="panel-card__header">
                    <div>
                        <h3 class="panel-card__title">Procesos con mÃ¡s errores</h3>
                        <p class="panel-card__subtitle">Ventana de 30 dÃ­as.</p>
                    </div>
                </div>
                <div class="table-wrap">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Proceso</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in _resumen.TopProcesses)
                            {
                                <tr>
                                    <td>@item.Label</td>
                                    <td>@item.Value</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </article>

            <article class="panel-card">
                <div class="panel-card__header">
                    <div>
                        <h3 class="panel-card__title">Usuarios con mÃ¡s errores</h3>
                        <p class="panel-card__subtitle">Ventana de 30 dÃ­as.</p>
                    </div>
                </div>
                <div class="table-wrap">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Usuario</th>
                                <th>Cantidad</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in _resumen.TopUsers)
                            {
                                <tr>
                                    <td>@item.Label</td>
                                    <td>@item.Value</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </article>
        </section>

        <section class="panel-card">
            <div class="panel-card__header">
                <div>
                    <h3 class="panel-card__title">Ãšltimos errores</h3>
                    <p class="panel-card__subtitle">Acceso rÃ¡pido a los incidentes mÃ¡s recientes.</p>
                </div>
                <a class="action-link" href="/auditoria/errores">Ver listado completo</a>
            </div>
            <div class="table-wrap">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Fecha</th>
                            <th>Proceso</th>
                            <th>Usuario</th>
                            <th>Error</th>
                            <th>DescripciÃ³n</th>
                            <th>AcciÃ³n</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in _latest)
                        {
                            <tr>
                                <td><a class="action-link" href="/auditoria/error/@item.Id">@item.Fecha.ToString("dd/MM/yyyy HH:mm")</a></td>
                                <td>@item.Proceso</td>
                                <td>@item.Usuario</td>
                                <td>@item.ErrorCodigo</td>
                                <td>@item.Descripcion</td>
                                <td>
                                    <a class="btn btn--ghost btn--sm" href="/auditoria/error/@item.Id">
                                        <i class="bi bi-box-arrow-up-right"></i>
                                        <span>Detalle</span>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </section>
    </div>
}

@code {
    private bool _loading = true;
    private AlfaCore.Models.AppUiMessage? _feedback;
    private AuditoriaResumenDto _resumen = new();
    private IReadOnlyList<AuditoriaErrorRowDto> _latest = [];

    protected override async Task OnInitializedAsync()
    {
        var result = await UiOps.RunAsync(async () =>
        {
            _resumen = await AuditoriaSvc.GetResumenAsync();
            _latest = await AuditoriaSvc.SearchErrorsAsync(new AuditoriaErrorFilterDto { MaxRows = 12 });
        }, "No se pudo cargar la auditorÃ­a.");

        _feedback = result.Feedback;
        _loading = false;
    }

    private static string GetFeedbackClass(AlfaCore.Models.AppUiMessage message)
        => message.Severity switch
        {
            AlfaCore.Models.AppUiFeedbackSeverity.Success => "conversations-inline--ok",
            AlfaCore.Models.AppUiFeedbackSeverity.Warning => "conversations-inline--error",
            AlfaCore.Models.AppUiFeedbackSeverity.Error => "conversations-inline--error",
            _ => "conversations-inline--ok"
        };
}
